// Placeholder App. The team-lead does NOT modify this file in the rn-counter fixture.
// The Counter component is added under components/ and not wired into App.tsx —
// the test only verifies that the component + its RNTL tests exist.
import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export default function App() {
  return (
    <SafeAreaView>
      <Text>rn-counter fixture</Text>
    </SafeAreaView>
  );
}
